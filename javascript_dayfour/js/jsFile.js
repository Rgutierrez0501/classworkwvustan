let  d= 15; //d -- is variable whose value 5
let  e=5; //e -- is a variable whose value 5 or 5.5
let f=0;

/**Variables can hold many type of data. In JS programming we call it data types.
1. numbers
2. strings -- sequence of characters
3. objects
**/
f=d+e; //+ its an addition operator = is an assignment operator
document.getElementById('id1').innerHTML="add ---"+f;
f+=d;// += .-= , *=, /=, %=, <<= ,>>=
document.getElementById('id2').innerHTML="addition assignment operator ---"+f;