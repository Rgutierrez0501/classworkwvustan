package abstractionexamples.interfaces;

/**
 * 
 * @author UD SYSTEMS
 * Interface is a blue print of a class It has static constants and abstract methods.
 * Its a mechanism to achieve abstraction in Java.	
 *
 */
public interface FirstInterface {
// There can be only abstract methods in the Java interface,not method body
	//Since Java 8 we can have default and static methods in an interface
	//Since Java 9 we can have private methods in an interface
	//You can only extend 1 abstract class but you can implement many interfaces
	//By interface, we can support the functionality of multiple inheritance
}
