package abstractionexamples.interfaces;

public interface DisplayInfo {
	//By default interface fields are public,static and final and the methods are public and abstract
	int MIN =5;
	void print();
}
